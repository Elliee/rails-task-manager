<%= form_for(@task) do |t| %>
  <div class="form-group">
    <%= t.label :title %>
    <%= t.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= t.label :details %>
    <%= t.text_area :details, class: 'form-control' %>
  </div>

    <div class="checkbox">
      <label>
        <%= t.check_box :completed %> Completed
      </label>
    </div>


  <%= t.submit class: 'btn btn-primary' %>
<% end %>
